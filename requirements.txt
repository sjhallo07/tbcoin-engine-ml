# TBCoin Engine ML - Lifecycle Management
# Python dependencies

# Core dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# No additional dependencies needed for the lifecycle management module itself
# as it uses only Python standard library features
